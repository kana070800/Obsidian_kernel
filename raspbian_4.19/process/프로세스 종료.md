1. exit 시스템 콜 호출시
2. 종료 시그널을 입력 받을 시

`__noreturn` >> 실행 후 자신을 호출한 함수로 되돌아가지 않는다 ex) do_exit() > schedule()
프로세스는 자신의 스택 메모리 공간을 해제할 수 없기 때문
	다음에 실행되는 프로세스가 종료되는 프로세스 메모리 공간을 해제

do_exit() 구현부
%%linux/kernel/exit.c%%
```
void __noreturn do_exit(long code)
{
	struct task_struct *tsk = current;
	int group_dead;
...
	if (unlikely(tsk->flags & PF_EXITING)) {
		pr_alert("Fixing recursive fault but reboot is needed!\n");
		
		tsk->flags |= PF_EXITPIDONE;
		set_current_state(TASK_UNINTERRUPTIBLE);
		schedule();
	}

	exit_signals(tsk);  /* sets PF_EXITING */
...
	exit_mm();
...
	exit_files(tsk);
	exit_fs(tsk);
...
	exit_notify(tsk, group_dead);
...
	do_task_dead();
...
```
task_struct 의 flags에 PF_EXITING 이 되어있다면 코드 실행
	do_exit 도중 do_exit 호출되었는지 점검
	tsk->flags |= PF_EXITPIDONE;
	task_struct 의 state에 TASK_UNINTERRUPTIBLE 지정
	schedule() 로 휴면

exit_signal 함수 도중 flags PF_EXITING 가 활성화된다
	종료할 프로세스가 처리할 시그널 존재시 retarget_shared_pending()을 통해 시크널 처리 프로세스 선정

프로세스 메모리 디스크립터인 mm_struct 리소스 해제
파일 디스크립터 정보 해제
exit_mm() > 부모에게 종료중이라는 사실 통지
do_task_dead() 호출
	여담으로 init 프로세의 종료시 커널 패닉 발생(파일시스템 디바이스 노드 생성 못했을때 등)

do_task_dead() 구현부
%%linux/kernel/sched/core.c%%
```
void __noreturn do_task_dead(void)
{
	/* Causes final put_task_struct in finish_task_switch(): */
	set_special_state(TASK_DEAD);

	/* Tell freezer to ignore us: */
	current->flags |= PF_NOFREEZE;

	__schedule(false);
	BUG();

	/* Avoid "noreturn function does return" - but don't continue if BUG() is a NOP: */
	for (;;)
		cpu_relax();
}
```
set_special_state()을 통해 프로세스 state를 TASK_DEAD로 전환
task_struct의 flags에 PF_NOFREEZE 적용

`__schedule(false)`는 선점 스케줄링 실행하지 않겠다는 의미이다

`__schedule()`구현부 >> 나중에 스케쥴링 때 자세히 링크
%%linux/kernel/sched/core.c%%
```
static void __sched notrace __schedule(bool preempt)
{
...
	if (likely(prev != next)) {
...
		rq = context_switch(rq, prev, next, &rf);
	} else {
		rq->clock_update_flags &= ~(RQCF_ACT_SKIP|RQCF_REQ_SKIP);
		rq_unlock_irq(rq, &rf);
	}
...
```
context_switch를 통해 컨텍스트 스위칭 실행

`context_switch()`구현부
%%linux/kernel/sched/core.c%%
```
static __always_inline struct rq *
context_switch(struct rq *rq, struct task_struct *prev,
	       struct task_struct *next, struct rq_flags *rf)
{
...
	switch_to(prev, next, prev);
	barrier();

	return finish_task_switch(prev);
}
```
finish_task_switch() 호출

finish_task_switch() 구현부
%%linux/kernel/sched/core.c%%
```
static struct rq *finish_task_switch(struct task_struct *prev)
	__releases(rq->lock)
{
...
	if (unlikely(prev_state == TASK_DEAD)) {
		if (prev->sched_class->task_dead)
			prev->sched_class->task_dead(prev);

		/*
		 * Remove function-return probe instances associated with this
		 * task and put them back on the free list.
		 */
		kprobe_flush_task(prev);

		/* Task is done with its stack. */
		put_task_stack(prev);

		put_task_struct(prev);
	}

	tick_nohz_task_switch();
	return rq;
}
```
state가 TASK_DEAD 일 때 put_task_stack()호출
	스텍 메모리 공간 해제하여 반환
put_task_struct() 호출하여 task_stuct 메모리 해제