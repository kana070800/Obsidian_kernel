
memory의 stack 공간의 절약은 일반적으로 생각안해도 된다(이미 결정된 공간)
	제귀호출과 같은 예외상황 존재

연산결과가 특정타입으로 귀결된다면 식으로 볼 수 있다.

if 문에서 무엇이 불가능한가? 고민

각 방법론의 성능 차이, 코드 길이 차이를 보고싶다면 디스어셈블리 사용 권장
	== 은 실수 방지를 위해 조심하자 >> [변수 == 0]  >  [0 == 변수]
	3항 연산자는 메모리 접근(mov)코드가 2줄 추가되어 좀 더 비용 (가독성 굿)
	 >=, > 비교 시 jle  or  jl 으로 차이 발생, zero flag 검사 유무
	 
재사용성, 모듈화 고려 필요
	성공확률에 따라 정렬 필요 (likely, unlikely 같은 건 뭘까???)

switch case 대신 배열 같은 자료구조를 활용하는것이 더 지향(flag)
	함수포인터?

메모리 특정 주소를 자주 읽어오는 경우 >>  volatile 권장
함수호출은 식

상위레벨 언어의 경우 예외처리를 고려한 인터페이스 및 시스템 설계가 중요해진다

함수호출은 식

배열이름은 포인터(요소1)이지만 &, sizeof 연산을 사용하면 포인터로 동작하지 않는다
	int a[5]; &a >> 배열 포인터, sizeof(a) >> 배열 바이트크기
	&a ==> int[5] (배열 전체)     sizeof(int) * 5  (보통 20바이트)

포인터(배열이름)의 용도
	참조
	정수 합차연산 >> byte단위가 아닌 각 요소단위 주소
	포인터 - 포인터 >> 사이 배열요소 개수 반환



땅따먹기
6각 타일
4개 군단 가능
점령지가 늘수록 매 턴 병사수 증가량 증가
군단끼리 충돌 시 큰 숫자의 병력에 비례하여 매 턴마다 동일 수 감소
병사수 적을수록 이동속도 감소