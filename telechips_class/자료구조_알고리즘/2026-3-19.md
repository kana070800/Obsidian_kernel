깊이 우선 탐색>> 재귀


너비 우선 탐색(bfs)
	최단거리, 최소비용, 인접구간 탐색 시 활용가능

bfs 절차
=
시작점
	몇개인가?
		여러개라면
			동시 사용하는가?
			한개씩 사용하는가?
인접여부
종료지점판단
큐의 설계(데이터)
방문표시
	입력 data 에 표시
	새 data(배열)에 표시 >> 보물섬
	최소비용 저장공간 요구 >> 등산로

알고리즘을 풀 때 문제가 말하고자 하는 바를 오해하지 말도록 주의


저글링(https://jungol.co.kr/problem/1078?cursor=OCw3LDE%3D)
```
#if 01

#define MAX_N (100+2)
int map[MAX_N][MAX_N];
int R, C;
int sR, sC; //start
int cnt = 0;

typedef struct _node {
	int r, c;
}node;
int front, rear;
node Queue[MAX_N * MAX_N];
void Enqueue(node data) {
	Queue[rear++] = data;
}

node Dequeue(void) {
	return Queue[front++];
}

void inputData(void) {
	char tmp[MAX_N] = { 0 };
	
	scanf("%d %d", &C, &R);

	for (int i = 1; i <= R; ++i) {
		scanf("%s", tmp + 1);
		for (int j = 1; j <= C; ++j) {
			map[i][j] = tmp[j] - '0';
			cnt += map[i][j];
		}
	}

	scanf("%d %d", &sC, &sR);
}
int dR[4] = { -1, 1, 0, 0 };
int dC[4] = { 0, 0, -1, 1 };

int BFS(int sR, int sC) {
	int nR, nC;
	int time;
	node curr;

	front = rear = 0;			//bfs 여러번 수행시 초기화 필요
	Enqueue((node) { sR, sC });
	map[sR][sC] = 3;			//방문표시

	while (front != rear) {				//q에 존재시 반복
		curr = Dequeue();
		time = map[curr.r][curr.c] + 1;
		for (int i = 0; i < 4; i++) {     //인접점 탐색 후 존재시 enqueue 및 방문표시
			nR = curr.r + dR[i];
			nC = curr.c + dC[i];

			if (map[nR][nC] == 1) {
				Enqueue((node) { nR, nC });
				map[nR][nC] = time;
			}
		}
	}
}

int main(void) {
	int N, M;
	//(void)freopen("j1078.txt", "r", stdin);
	inputData();

	int a = BFS(sR, sC);

	//printf("%d\n%d", BFS(sR, sC), cnt - rear);  
	//내부에서 rear 입력 후 BFS가 실행되면 잘못된 정보가 나오기에 해당사항 고려필요
	printf("%d\n%d", a, cnt - rear);

	return 0;
}


#endif
```