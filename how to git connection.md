git init
git branch -M main
git remote add origin "주소"
git remote -v    //check
git config --global user.email "이메일"
git config --global user.name "이름"

타 기기에서 엑세스

git clone "주소"
git config --global user.email "이메일"
git config --global user.name "이름"